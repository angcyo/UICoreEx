apply from: allGradle.libBase

dependencies {
    //implementation project(allModule.core)
    //implementation project(allModule.glide)
    //implementation project(allModule.transition)
    //implementation project(allModule.objectbox_lp) //数据库

    implementation project(allModule.dslitem)
    implementation project(allModule.dialog)

    api project(allModule.dialog2)
    api project(allModule.canvas)

    api 'com.angcyo:opencv:hingin-SNAPSHOT'
}