apply from: allGradle.libBase
android {
    //AGP 8.x 必须指定命名空间
    namespace 'com.angcyo.um.apm'
}
dependencies {
    implementation project(allModule.library)
    api project(allModule.umeng)

    //性能监控
    api 'com.umeng.umsdk:apm:1.3.1' // apm包依赖
}